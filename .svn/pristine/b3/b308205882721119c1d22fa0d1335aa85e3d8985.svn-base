import { TestBed } from '@angular/core/testing';

import { BusgenericserviceService } from './busgenericservice.service';

describe('BusgenericserviceService', () => {
  let service: BusgenericserviceService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(BusgenericserviceService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
